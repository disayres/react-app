// import PropTypes from "react/prop-types"

export default function Comments(props) {
    return (
        <section>
            <h3>{props.title}</h3>
            <p>{props.text}</p>
        </section>
    )
}
